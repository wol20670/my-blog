{
  // assets/css/jekyll-theme-chirpy.scss 는 Jekyll이 먼저 Liquid({%- if -%})를 치환한 뒤에야
  // Sass로 넘어간다. VS Code 내장 SCSS validator는 Liquid를 모르기 때문에
  // "알 수 없는 키워드 / { 필요" 같은 가짜 오류를 표시한다. 실제 빌드 오류가 아니다.
  // 이 저장소의 .scss 파일은 그 하나뿐이라 끄더라도 잃는 것이 없다.
  "scss.validate": false
}
